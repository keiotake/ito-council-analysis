# 毎月定期チェック セットアップガイド

毎月1日に自動的に議事録の更新確認・反映を行う仕組み。

---

## 📋 何が自動化されるか

1. ✅ 公式サイトから最新の議事録一覧を取得
2. ✅ 前回との差分から新規議事録を検出
3. ✅ 新規分の本文HTMLをダウンロード
4. ✅ パース・データ統合
5. ✅ HTMLビルド
6. ✅ GitHubにコミット＆プッシュ（自動デプロイ）
7. ✅ 実行ログを `logs/` に保存

---

## 🚀 手動実行（まず動作確認）

ダブルクリックで実行できます：

```
C:\Users\ka\ito-council-summary\monthly_check.bat
```

またはコマンドラインから：

```bash
cd C:\Users\ka\ito-council-summary
node monthly_check.js
```

実行後、`logs/monthly_check_YYYY-MM-DD.log` に詳細ログが残ります。

---

## ⏰ 毎月1日に自動実行する設定（Windowsタスクスケジューラ）

### セットアップ手順

1. **タスクスケジューラを起動**
   - Windowsキー → 「タスクスケジューラ」と入力 → Enter

2. **基本タスクの作成**
   - 右側の「**基本タスクの作成**」をクリック

3. **名前**：
   - 名前：`みんなの伊東市 - 毎月定期チェック`
   - 説明：`毎月1日に伊東市議会の議事録更新を自動取得・デプロイ`

4. **トリガー**：
   - **毎月** を選択
   - 開始：来月の1日 09:00（任意の時刻）
   - 月：すべて選択
   - 日：**1日** のみチェック

5. **操作**：
   - **プログラムの開始** を選択
   - プログラム/スクリプト：
     ```
     C:\Users\ka\ito-council-summary\monthly_check.bat
     ```
   - 開始（オプション）：
     ```
     C:\Users\ka\ito-council-summary
     ```

6. **完了** をクリック

### 動作テスト

タスクスケジューラの一覧から作成したタスクを右クリック → **実行** で手動テストできます。

---

## 📧 実行結果をメール通知（オプション）

メールで実行結果を受け取りたい場合は、`monthly_check.js` の最後に以下を追加：

```javascript
// メール送信（nodemailer等を使う場合）
const nodemailer = require('nodemailer');
// 設定は別途
```

---

## 🛡️ 想定されるトラブルと対処

| エラー | 原因 | 対処 |
|---|---|---|
| `git push` が失敗 | リモートとの競合 | `git pull --rebase` してから手動push |
| 議事録取得が `[busy]` で止まる | サーバー側のレート制限 | 数時間後に再実行 |
| node コマンドが見つからない | Node.jsのPATH | 環境変数で `node` を通す |
| 文字化け | Shift_JIS変換失敗 | `iconv-lite` パッケージを再インストール |

---

## 📅 推奨運用

| 時期 | 期待される更新 |
|---|---|
| **4月初旬** | 3月定例会の議事録（年度末議案） |
| **7月初旬** | 6月定例会の議事録 |
| **10月初旬** | 9月定例会の議事録 |
| **1月初旬** | 12月定例会の議事録 |

通常、議事録は本会議終了から **2〜3ヶ月後** に公開されます。

---

## 🔍 ログの確認方法

```bash
cd C:\Users\ka\ito-council-summary
ls -la logs/
cat logs/last_check.json
```

最新の `monthly_check_*.log` を開けば、その月の実行詳細が確認できます。

---

## 🤖 Claude Code から手動チェック

Claude Code を開いて以下のように依頼することもできます：

```
「最新の議事録があるか確認して、あれば取得・統合・デプロイして」
```

または、簡単に：

```
「毎月チェックを実行」
```

すると Claude が `node monthly_check.js` を実行してくれます。
