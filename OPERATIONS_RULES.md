# みんなの伊東市 - 運用ルール

「みんなの伊東市」は伊東市議会の公式情報を扱う非公式サイトです。  
誤った情報や不適切な表現が公開されることは、議員・市民・運営者すべてに不利益をもたらします。  
本書はそれを防ぐための日常運用ルールをまとめたものです。

---

## 🛡️ 必須ルール

### ルール1: 月次定期チェックの結果を必ず確認する

**毎月1日（タスクスケジューラで自動実行）** または **手動** で `monthly_check.js` が走った後、  
ログを必ず確認します。

```bash
# 手動実行する場合
npm run monthly
# または
node monthly_check.js

# 最新ログを確認
cat logs/monthly_check_$(date +%Y-%m-%d).log
```

**確認ポイント:**
- `CRITICAL` 件数が **0件** であること
- `WARN` 件数があれば一つ一つ目視確認
- ビルド時の `[AUDIT WARN]` メッセージがないこと

**CRITICAL検出時の動作:**
- `monthly_check.js` は**自動的にビルド・デプロイを中止**します
- `logs/audit_findings_YYYY-MM-DD.json` に詳細が保存されます
- 人手で修正してから再実行する必要があります

### ルール2: 新しい議事録追加時は必ずビルドログをチェックする

手動で議事録を追加した時は、ビルド後に以下を実行：

```bash
node build_v3.js 2>&1 | grep "AUDIT WARN"
# または
npm run audit
```

何も出力されなければOK。出力された場合はルール3へ。

### ルール3: 怪しい表現を見つけたら即修正する

#### 即時対応フロー

```
[発見] CRITICALまたはWARN
   ↓
[確認] YouTube字幕と元動画で音声を確認
   ↓
[判断] 音声認識ミス? それとも議事録の事実引用?
   ↓
   ├─ 音声認識ミス → 該当データを修正 → 再ビルド
   └─ 事実引用     → 文脈をしっかり保つため、そのまま残す or 注釈追加
```

#### 修正手順（音声認識ミスの場合）

1. 該当データファイルを特定（通常は以下のどれか）：
   - `analysis_data.json` — 動画タイトル等
   - `question_summaries.json` — 質問要約
   - `video_summaries.json` — 動画要約
   - `topics_cache.json` — トピックキャッシュ

2. テキストエディタで開いて該当箇所を修正

3. 再ビルド & デプロイ
   ```bash
   node build_v3.js
   npm run audit  # クリーンになったか確認
   git add -A && git commit -m "音声認識ミス修正：XXX → YYY"
   git push
   ```

---

## 📋 監査コマンド一覧

| コマンド | 用途 |
|---|---|
| `npm run audit` | データを監査して結果を表示（許可済みは除外） |
| `npm run audit:show-all` | 許可済みも含めて全件表示 |
| `npm run audit:approve-all` | 現在の全findingsを許可リストに登録（ベースライン作成用） |
| `npm run audit:json` | JSON形式で出力（プログラム連携用） |
| `npm run audit:strict` | CRITICAL検出時に exit code 1（CI用） |
| `npm run monthly` | 議事録取得 → 監査 → ビルド → デプロイの一括実行 |

### 許可リストの仕組み

新規データを取り込んだ後、`npm run audit` を実行すると、**新規ヒットのみ**が表示されます。

事件・歴史的引用など、人手で確認して問題ないとわかったものは、`audit_allowlist.json` に登録されており、次回からはスキップされます。

初回ベースライン作成は `npm run audit:approve-all` で実施済み（既存16件の事件引用を登録）。

新規ヒットが検出された場合：
- 1件ずつ目視確認
- 音声認識ミスなら修正してビルドし直す
- 事件引用など正当なものなら、確認後に`audit_allowlist.json` を手動で追記、または再度`audit:approve-all`

---

## 🔍 監査が検出するパターン

### CRITICAL（重大・必ず確認）
| 語 | 想定されるミス |
|---|---|
| 即死 | 「促進」「即時」等の音声認識ミス |
| 殺害 | 「察知」「殺到」等の音声認識ミス、または事件引用 |
| 虐殺 | 「虐げる」「逆境」等の音声認識ミス |
| 処刑 | 音声認識ミス |
| 抹殺 | 音声認識ミス（特に人物文脈で危険） |
| 暗殺 | 音声認識ミス |
| 撲殺 | 音声認識ミス |
| 射殺 | 音声認識ミス |

### WARN（軽度・要確認）
| 語 | 想定されるミス |
|---|---|
| 無能 | 人物評価表現 |
| 失格 | 人物評価表現 |
| クズ | 誹謗中傷表現 |
| バカ | 誹謗中傷表現 |
| アホ | 誹謗中傷表現 |
| 詐欺師 | 誹謗中傷表現 |

**追加したい語があれば** `audit.js` および `monthly_check.js` の `AUDIT_RULES` を編集してください。

---

## ⚠️ 「事実引用」の判断基準

議員が議会で過去の事件・歴史を引用している場合、「殺害」「射殺」等の語が出ることはあります。  
これらは**事実引用として正当**であり、削除すべきではありません。

**そのままで良い例:**
> 「平成9年は…児童殺害事件や大手証券会社の不祥事件があり…」 — 事件の歴史的引用

**修正が必要な例:**
> 「離職した有資格者の復職**即死**について…」 — 「復職促進」の音声認識ミス

迷ったら **YouTube動画の該当箇所を実際に視聴**して、議員の発言と一致するか確認してください。

---

## 📅 定例運用カレンダー

| 時期 | 作業 |
|---|---|
| **毎月1日** | `monthly_check.js` 自動実行 → ログとaudit結果を確認 |
| **議事録新着時** | （4月・7月・10月・1月） ビルドログ・audit結果を確認 |
| **手動編集後** | `npm run audit` で再確認 |
| **公開コンテンツ修正時** | 公開前に `npm run audit:strict` |

---

## 🚨 緊急時の対応

### サイトに不適切表現が公開されてしまった場合

1. **即座にサイトから削除**
   - 該当データファイルを修正 → `node build_v3.js` → `git commit && git push`
   - GitHub Pagesへの反映は数十秒〜1分

2. **GoogleキャッシュからもURLを削除依頼**
   - https://search.google.com/search-console/removals でURLを指定して削除リクエスト

3. **議員ご本人への謝罪**
   - 議員に名前を冠した不適切表現が表示された場合、運営者として速やかに連絡・謝罪
   - 連絡先: 議員一覧タブの議員情報を参照

4. **再発防止**
   - 監査ルールに該当パターンを追加
   - `audit.js` の `AUDIT_RULES` を更新

---

## 📞 連絡先

運営: **伊東市議会議員 大竹 圭**  
メール: **ka@oh-life.co.jp**
